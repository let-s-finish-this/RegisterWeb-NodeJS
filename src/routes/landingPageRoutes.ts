import express from "express";
import { RouteLookup } from "../controllers/lookups/stringLookup";
import * as LandingPageRouteController from "../controllers/landingPageRouteController";

function landingPageRoutes(server: express.Express) {

}

module.exports.routes = landingPageRoutes;
